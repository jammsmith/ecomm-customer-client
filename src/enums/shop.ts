export enum CurrencyEnum {
  GBP = 'GBP',
  EUR = 'EUR',
  USD = 'USD',
}

export enum DeliveryZoneEnum {
  uk = 'uk',
  europe = 'europe',
  us = 'us',
}

export enum StripeElementStatusEnum {
  LOADING = 'loading',
  READY = 'ready',
  COMPLETE = 'complete',
  SUBMITTING = 'submitting',
  ERROR = 'error',
}
